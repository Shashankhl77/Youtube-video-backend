services:
  - type: web
    name: downloader
    env: node
    buildCommand: |
      apt-get update
      apt-get install -y ffmpeg python3 python3-pip
      pip3 install yt-dlp
      npm install
    startCommand: node src/index.js